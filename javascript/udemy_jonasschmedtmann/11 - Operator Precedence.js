var now = 2018;
var yearJohn = 1989;
var fullAge = 18;

// Multiple Operators
var isFullAge = now - yearJohn >= fullAge; //true

// Grouping
var ageJohn = now - yearJohn;
var ageMark = 35;
var average = (ageJohn + ageMark) / 2;

// Multiple assignments
var x, y;
var x = y = (3 + 5) * 4 - 6; // 8*4 -6 // 32-6 // 26
// assignment works right to left

// More operators
x = x * 2;
x *= 2;

// Increment
x = x +1;
x += 1;
x++ // adds 1 unit to the variable